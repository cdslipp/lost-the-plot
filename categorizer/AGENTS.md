# Categorizer Agent Instructions
See root `AGENTS.md` for general project mandates.

## Categorizer Specifics
- This is a developer tool for enriching asset metadata.
- Reads from `app/static/final_assets/items.json`.
- Used to classify `item_type`, subcategories, and technical specs (mic vs DI, phantom power, etc).
